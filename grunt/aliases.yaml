default:
  - build

init:
  - config
  - shell:install
  - update
  - build

config:
  - replace
  - rename

build:
  - lint
  - test

generate:
  - makepot

update:
  - clean:lib
  - shell:update
  - copy
  - build

lint:
  - jslint
  - phplint

test:
  - phpunit

release:
  - lint
  - test
  - build
  - generate
  - replace:project_version